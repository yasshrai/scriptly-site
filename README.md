# Scriptly 🚀

**Scriptly** is a lightweight, transparent Linux utility designed for developers and power users who want to install applications from official sources without the bloat of traditional package managers or the complexity of manual setups.

Built with simplicity in mind, Scriptly provides a collection of structured, standalone shell scripts that handle dependency checks, downloads, extraction, and symlinking automatically.

